# ============================================
# 1. PersistentVolumeClaim (logs persistentes)
# ============================================
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: microdom-logs-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
---
# ============================================
# 2. ConfigMap (configuración no sensible)
# ============================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: microdom-config
data:
  microdom.conf: |
    REMOTE_HOST=witchblade.com.ar
    REMOTE_PORT=443
    REMOTE_PROTO=https

---
# ============================================
# 3. Service (ClusterIP en puerto 8080)
# ============================================
apiVersion: v1
kind: Service
metadata:
  name: microdom-service
  labels:
    app: microdom
spec:
  type: NodePort
  selector:
    app: microdom
  ports:
    - name: http
      nodePort: 30141
      port: 8080
      targetPort: 8080
---
# ============================================
# 4. Ingress (host → puerto 8080)
# ============================================
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: microdom-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: microdom.witchblade.com.ar
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: microdom-service
                port:
                  number: 8080
---
# ============================================
# 5. Deployment (limpio, optimizado, sin subPaths)
# ============================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: microdom
  labels:
    app: microdom
    tier: backend
    version: "1.0"
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: microdom
  template:
    metadata:
      labels:
        app: microdom
        tier: backend
    spec:
      serviceAccountName: default
      terminationGracePeriodSeconds: 10

      containers:
        - name: microdom
          image: ghcr.io/wpirri/microdom:latest
          imagePullPolicy: Always

          # 🔥 Variables de entorno desde el Secret
          env:
            - name: DBHOST
              valueFrom:
                secretKeyRef:
                  name: microdom
                  key: DBHOST

            - name: DBNAME
              valueFrom:
                secretKeyRef:
                  name: microdom
                  key: DBNAME

            - name: DBUSER
              valueFrom:
                secretKeyRef:
                  name: microdom
                  key: DBUSER

            - name: DBPASSWORD
              valueFrom:
                secretKeyRef:
                  name: microdom
                  key: DBPASSWORD
          ports:
            - containerPort: 8080

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          # 🔥 Solo logs y configMap, sin Secret mounts
          volumeMounts:
            - name: logs
              mountPath: /app/logs
            - name: etc
              mountPath: /app/etc
            - name: html-volume
              mountPath: /app/html
            - name: download-volume
              mountPath: /app/download
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5

      volumes:
        - name: logs
          persistentVolumeClaim:
            claimName: microdom-logs-pvc

        - name: etc
          configMap:
            name: microdom-config

        - name: html-volume
          hostPath:
            path: /var/lib/microdom/html
            type: DirectoryOrCreate
        - name: download-volume
          hostPath:
            path: /var/lib/microdom/download
            type: DirectoryOrCreate
      imagePullSecrets:
        - name: ghcr
---
# ============================================
# 6. Secret (credenciales)
# ============================================
apiVersion: v1
kind: Secret
metadata:
  name: microdom
type: Opaque
stringData:
  DBUSER: "dompi_web"
  DBPASSWORD: "dompi_web"
  DBHOST: "192.168.10.32"
  DBNAME: "DB_DOMPIWEB"
  